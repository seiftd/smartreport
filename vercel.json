{
  "version": 2,
  "builds": [
    {
      "src": "backend/test-server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "backend/test-server.js"
    }
  ],
  "installCommand": "cd backend && npm install",
  "buildCommand": "cd backend && echo 'Backend build completed'",
  "outputDirectory": "backend"
}
